- name: create folder
  file:
    path: ~/web
    state: directory

# - name: copy artifact files 
#   template:
#     src: 
#       files/artifact.tar.gz
#     dest: ~/

- name: extract artifact tar file 
  unarchive:
    src: 
      files/artifact.tar.gz
    dest: ~/web

- name: Executing node
  shell: |
    cd ~/web
    npm install 
    pm2 stop default
    pm2 start npm --start 